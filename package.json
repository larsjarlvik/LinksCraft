{
  "name": "links-craft",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --outdir=public/js --bundle --sourcemap --minify --loader:.wgsl=file",
    "start": "npm run build -- --servedir=public",
    "check": "biome check --apply ./"
  },
  "author": "Lars Jarlvik",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@biomejs/biome": "1.6.4",
    "esbuild": "0.20.2"
  },
  "dependencies": {
    "@webgpu/types": "^0.1.40",
    "gltf-loader-ts": "^0.3.1",
    "webgpu-utils": "^1.7.0",
    "wgpu-matrix": "^2.8.0"
  }
}
